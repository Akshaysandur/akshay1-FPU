# FPU Job Scheduler

Run the new simplified app separately from the old dashboard:

```powershell
python -m streamlit run mentor_app.py
```

This version focuses on:

- adding orders
- building operations step by step
- scheduling orders with static or dynamic mode
- stepping through execution details

It does not include any factory visualization.
